# دليل إدارة المنيو (MENU_GUIDE_AR) 📋

هذا الدليل يشرح لك كيفية التحكم الكامل في المنيو الخاص بك من خلال التعديل على ملف البرمجة الأساسي المسئول عن البيانات وهو الملف الموجود في المسار التالي:
`src/constants.ts`

---

## 🚀 1. كيفية إضافة تصنيف جديد (Category)

التصنيف هو المجموعة التي تندرج تحتها الأصناف (مثل: البيتزا، العصائر، الحلويات).

**الخطوات:**
1. افتح ملف `src/constants.ts`.
2. ابحث عن كائن `UI_STRINGS`.
3. أضف الكلمة المفتاحية للتصنيف الجديد باللغة الإنجليزية في قسم `ar` وقسم `en`.

**مثال: إضافة تصنيف "ساندوتشات":**
في كائن `UI_STRINGS`:
```typescript
categories: {
  all: 'الكل',
  pizza: 'البيتزا',
  pies: 'الفطائر الشامية',
  juices: 'العصائر',
  sandwiches: 'الساندوتشات' // أضف هذا السطر هنا
},
```
وتأكد من إضافته أيضاً في قسم `en`:
```typescript
categories: {
  all: 'All',
  pizza: 'Pizza',
  // ...
  sandwiches: 'Sandwiches' // أضف هذا السطر هنا
}
```

---

## 🍔 2. كيفية إضافة صنف جديد (Item)

الأصناف تضاف داخل مصفوفة `MENU_DATA`.

**الخطوات:**
1. ابحث عن مصفوفة `MENU_DATA`.
2. انسخ كود صنف قديم والصقه في نهاية القائمة.
3. قم بتغيير البيانات (الاسم، السعر، الصورة، التصنيف).

**نموذج الكود لإضافته:**
```typescript
{
  id: 17, // تأكد أن الرقم فريد وغير مكرر
  category: 'sandwiches', // يجب أن يطابق اسم التصنيف الذي أضفته أعلاه
  translations: {
    ar: { name: 'ساندوتش برجر', description: 'برجر لحم بقري فاخر مع الجبن' },
    en: { name: 'Beef Burger', description: 'Premium beef burger with cheese' }
  },
  price: 8000, // السعر بالرقم فقط
  image: 'رابط_الصورة_هنا' 
},
```

---

## 💰 3. تعديل الأسعار والأسماء

لتعديل أي صنف موجود بالفعل:
1. ابحث عن الصنف داخل `MENU_DATA` باستخدام اسمه.
2. **لتعديل السعر:** غيّر الرقم الموجود بجانب `price`.
3. **لتعديل الاسم:** غيّر النص الموجود داخل `translations.ar.name` للغة العربية.
4. **إذا كان للصنف أحجام (Variants):** ستجد مصفوفة باسم `variants` تحتوي على الأسعار لكل حجم (وسط، كبير، عائلي)، قم بتعديل الأرقام بداخلها.

---

## 🖼️ 4. إضافة وتغيير الصور (باستخدام المجلدات)

أفضل طريقة لتنظيم صورك هي وضعها داخل مجلد مخصص.

**الخطوات:**
1. اذهب إلى مجلد `public` الموجود في جذر المشروع.
2. قم بإنشاء مجلد جديد بداخله وسمّه `images` (إذا لم يكن موجوداً).
3. قم برفع صورك داخل هذا المجلد (مثلاً: صورة باسم `burger.jpg`).
4. في الكود، استدعِ الصورة هكذا: `image: '/images/burger.jpg'`.

**لماذا هذه الطريقة؟**
*   لأن المجلد `public` يظهر مباشرة على الإنترنت، فأي شيء بداخله يمكنك الوصول إليه بوضع `/` ثم اسم الملف.
*   تساعدك على تنظيم مشروعك بدلاً من وضع كل الصور في مكان واحد.

---

## 📍 5. أين أضع الكود بالضبط؟ (تحديد المكان)

إذا كنت تتساءل أين تبدأ وأين تنتهي عند نسخ الكود:

1.  **لإضافة صنف جديد:** اذهب إلى ملف `src/constants.ts` وابحث عن القوس المربع الذي يغلق المصفوفة `export const MENU_DATA = [...]`. أضف صنفك الجديد قبل القوس المربع الأخير `];`.
2.  **الفواصل الهامة:** تأكد أن كل صنف ينتهي بفاصلة `,` هكذا:
    ```typescript
    {
      id: 1,
      // ... باقي البيانات
    }, // <-- هذه الفاصلة ضرورية جداً إذا كنت ستضيف صنفاً آخر بعدها
    ```

---
المنيالمنيوالمنيو والمنيو
## 🖼️ 6. كيفية تغيير صورة صنف موجود (تحديث)

إذا أردت تغيير صورة صنف موجود أصلاً في المنيالمنيوو بصورة جديدة قمت برفعها:

1.  **اسم الصورة:** تأكد من معرفة اسم الصورة بدقة والإمداد الخاص بها (مثل: `burger_new.png` أو `moala.jpg`).
2.  **تحديد المكان:** اذهب إلى ملف `src/constants.ts` وابحث عن الصنف المطلوب.
3.  **التعديل:** ستجد سطراً يبدأ بكلمة `image`. قم بحذف الرابط القديم واكتب المسار الجديد الذي يبدأ بـ `/images/`.

**مثال قبل التعديل (رابط خارجي):**
```typescript
image: 'https://images.unsplash.com/photo-1513104890138-7c749659a591',
```

**مثال بعد التعديل (صورتك المحلية):**
```typescript
image: '/images/burger_new.png', // تأكد من وجود علامة / في البداية
```

---

## 💡 نصائح هامة للتحكم الاحترافي:
*   **معرف الصنف (id):** تأكد دائماً أن كل صنف له رقم `id` مختلف عن الآخرين.
*   **الفواصل:** تأكد من وجود فاصلة `,` بعد كل خاصية وبعد كل كائن `{}`.
*   **اللغة الإنجليزية:** المنيو يدعم لغتين، فاحرص دائماً على كتابة الاسم باللغة العربية والإنجليزية لضمان ظهور المنيو بشكل صحيح عند تبديل اللغة.

---
**تم إعداد هذا الدليل بواسطة Ezoo-Tech لمساعدتك في إدارة تطبيقك بكل سهولة.** 🌟
